/**
 * @axag/angular — register an agent action for as long as the element is in the view.
 */

export { AxagDirective } from './axag.directive.js';
export { defineAction } from '@axag/core';
export type { ActionSpec } from '@axag/core';
